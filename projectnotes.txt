Title of Project:
-Scale Navigator

Libraries
-Node.js, Express, TypeScript, nodemon, ejs

Project description:
-Build a web app to select musical chords and recommend scales.

Goal of the project:
-To create a dynamic musical keyboard to select the appropriate chord and scale for musicians and nonmusicians.

Bash commands
npm run build
run npm devStart
run npm start
npx tailwindcss -i ./src/input.css -o ./src/output.css --watch


----------------------------------------------------------------------------------------------------
Notes

-07_09_24
Created project folder,
Ran bash npm init -y,
Ran bash npm i express,
Ran bash  npm install --save-dev typescript @types/node @types/express ts-node,
Created src folder,
Created index.ts in src folder,
Created tsconfig.js in root of project folder,
Created server.ts in src folder,
Created the folder dist,
Updated package.json script key "script": { "start": ts-node src/server.ts", "build: "tsc", "server": node dist/server.js" },
Updated server.ts with import statement for express and defined const app = express(),
Updated server.ts with app.use(express.json()) method call,
Updated server.ts with app.get('/', (req: Request, res: Response) => { res.send('Hello World Scale Navigator')}),
Updated server.ts with app.listen(port, () => { console.log(`Server is running on http://localhost:${port}`)},
Ran bash npm start and verfied in console Server is running on http://localhost:3000,


-07_11_24
Ran bash npm i nodemon,

-07_12_24
Ran bash npm i ejs,
Updated package.json script key "devStart": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts",
Ran bash npm i --save-dev ts-node-dev,
Updated package.json "scripts": key with "devStart": "ts-node-dev --respawn --transpile-only src/server.ts",

-07_16_24
Ran bash i --save-dev @types/ejs,
Updated src/server.ts added app.set('view engine', 'ejs');,
Created src/views folder,
Created src/views/index.ejs,
Updated src/views/index.ejs with HTML boilerplate,
Updated src/server.ts with app.set('views', path.join(__dirname, 'views'));,
Updated src/server.ts with app.get() {    res.render('index', { title: 'Hello World Scale Navigator' });},

-07_18_24
Ran bash -D tailwindcss,
npx tailwindcss init,
Updated tailwind.config.js content: [] array with ./src/**/*.{html,js,ts,tsx}",
Created src/input.css  with the directives @tailwind base; @tailwind components; @tailwind utilties;,
Ran bash npx tailwindcss -i ./src/input.css -o ./src/output.css --watch,
Updated src/server.ts app.use(express.static(path.join(__dirname, '../src')));,
Updated src/views/index.ejs <link> element to <link rel="stylesheet" href="/output.css">,
Updated src/views/index.ejs with <script> element,
Updated src/views/index.ejs with canvas element,

-07_19_24
Updated src/views/index.ejs with new <script> element to fix white key spacing bug,

-07_22_24
Created src/drawkeyboard.ts,
Updated src/views/index.ejs with a <script> element to the drawkeyboard.ts,
Updated tsconfig.json with key "rootDir": "./src",
Updated dist/drawkeyboard.ts with type asserition to set the const canvas as HTMLCanvasElement | null,
Updated dist/drawkeyboard.ts with early returns and if(!canvas) and if(!ctx) to resolve bugs,
Updated src/views/index.ejs removed <script> element and backed up to notes,

-07_23_24
Updated src/views/index.ejs <style> element body rule with flex-direction: column, align-items: center,
Updated src/views/index.ejs <canvas> element attribute properties width="350" and height="100",
Updated src/drawkeyboard.ts const white and black key definition whiteKeyWidth = 350 / 14, whiteKeyHeight = 100, blackKeyWidth = whiteKeyHeight * 0.6, blackKeyHeight = whiteKeyHeight * 0.6,
Ran bash git init,
Created .gitignore,
Ran bash git remote add origin https://github.com/emprexvest/ScaleNavigator.git,
Updated .gitignore with node_modules/,
Ran bash git remote -v,
Ran bash git push -u origin main,
Ran bash git rm -r --cached node_modules/,
Ran bash git commit -m "Remove node_modules from tracking",
Ran bash git push,
Updated tailwind.config.js to include extend: key with animation properties and keyframe properties,
Updated src/drawkeyboard.ts const keyElements: {[key: number]: { type: string, x: number, width: number } } = {};,
Updated src/drawkeyboard.ts for() { const x = whiteKeyIndex * whiteKeyWidth },
Updated src/drawkeyboard.ts for() { keyElements[i] = { type: 'white', x, width: whiteKeyHeight } },
Updated src/drawkeyboard.ts for() { const x = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;},
Updated src/drawkeyboard.ts for() {  ctx.fillRect(x, 0, blackKeyWidth, blackKeyHeight); }},
Updated src/drawkeyboard.ts for() { keyElements[i] = { type: 'black', x, width: blackKeyWidth }; },
Updated tsconfig.json with a new key  "lib": ["dom", "ES6"],
Updated src/drawkeyboard.ts added canvas.addEventListener() {for (){if () } else if {}},
Updated src/drawkeyboard.ts function highlightKey() { create a div and animation, including setTimeout method to remove div},
Updated src/drawkeyboard.ts function highlightKey()  document.body.appendChild(highlight);,
Updated src/views/index.ejs added <div class="canvas-container"> to nest <canvas> element,
Updated src/views/index.ejs <style> .canvas-container { position: relative; },
Updated src/drawkeyboard.ts function highlightKey(x: number, y: number, width: number, height: number, canvasLeft: number, canvasTop: number) { highlight.style.left = `${canvasLeft + x}px`; highlight.style.top = `${canvasTop + y}px`;,

-07_24_24
Created src/views/dropdownmenu.ejs
Updated src/views/dropdownmenu.ejs created <div> with nested <label> and <select>, with nested <option>,
Updated src/views/dropdownmenu.ejs added Tailwind CSS classes for styling the dropdown menus,
Updated src/views/index.ejs with a <div> and an EJS <%- include(dropdownmenu) %>,

-07_25_24
Updated src/drawkeyboard.ts with const noteSelect and const chordTypeSelect,
Updated src/drawkeyboard.ts if() error handling with (!noteSelect || !chordTypeSelect),
Updated src/drawkeyboard.ts function highlightKey() argument type asserition (isTemporary: boolean),
Updated src/drawkeyboard.ts canvas.addEventListener('click') for() nested if() arguments body highlightKey() method with new isTemporary parameter value of true,
Updated src/drawkeyboard.ts if else logic to nest the setTimeout() method and else, with a nested if(ctx) to change the ctx.fillStyle = 'red';, .fillRect(), strokeRect(),
Updated src/drawkeyboard.ts function highlightScale(note:string, chordType: string) {} with a error checking for null ctx and canvas elements,
Updated src/drawkeyboard.ts function highlightScale() ctx.clearRect(),
Updated src/drawkeyboard.ts function highlightScale() if() to redraw the keyboard,
Updated src/drawkeyboard.ts function highlightScale() const noteIndices = { 'C': 0, ... },
Updated src/drawkeyboard.ts function highlightScale() const majorScalePattern = [0, 2, 4, 5, 7, 9, 11],
Updated src/drawkeyboard.ts function highlightScale() const startIndex = noteIndices[note as keyof typeof noteIndices];,
Updated src/drawkeyboard.ts majorScalePattern.forEach(interval => { const keyIndex = (startIndex + interval) % 12; const x = keyElements[keyIndex].x; const width = keyElements[keyIndex].width; const height = keyElements[keyIndex].type === 'white' ? whiteKeyHeight : blackKeyHeight;);

-07_26_24
Updated src/drawkeyboard.ts Added comment // Reset value to 0,
Updated src/drawkeyboard.ts function highlightKey console.log(`Highlighting key at position (${x}, ${y}), size (${width}x${height}), temporary: ${isTemporary}`);,
Updated src/drawkeyboard.ts function highlightScale console.log(`Highlighting scale for note: ${note}, chord type: ${chordType}`);,
Updated src/drawkeyboard.ts function highlightScale  console.log(`Start index for note ${note}: ${startIndex}`);,
Updated src/drawkeyboard.ts majorScalePattern.forEach() console.log(`Highlighting key index: ${keyIndex}, x: ${x}, width: ${width}, height: ${height}`);,
Updated src/drawkeyboard.ts majorScalePattern.forEach() noteSelect.addEventListener() { const note = noteSelect.value; const chordType = chordTypeSelect.value; highlightScale(note, chordType);});,
Updated src/drawkeyboard.ts majorScalePattern.forEach() chordTypeSelect.addEventListener() { const note = noteSelect.value; const chordType = chordTypeSelect.value; highlightScale(note, chordType);});,

-08_01_24
Commented commented out src/drawkeyboad.ts draw white key and draw black key for loops,
Updated src/drawkeyboard.ts refactored the draw white key and draw black key into function drawKeyboard ();
Updated src/drawkeyboard.ts added drawKeyboard() method call,
Updated src/drawkeyboard.ts added drawKeyboard() method call to the highlightScale() fuction body,
Commented commented out src/drawkeyboard.ts redraw keyboard for () loop for the keyboard from the highlightScale() {} body,
Commented commented out src/drawkeyboard.ts highlight scale majorScalePattern.forEach(),
Updated src/drawkeyboard.ts majorScalePattern.forEach(interval => { const keyIndex..},
Updated src/drawkeyboard.ts added type assertion to drawKeyboard() fuction (ctx as CanvasRenderingContext2D)...,

-08_05_24
Updated src/drawkeyboard.ts updated const whiteKeyWidth = (350 / 14) * 2; and const whiteKeyHeight = 100 * 2;,
Updated src/drawkeyboard.ts added set canvas dimensions canvas.width = whiteKeyWidth * (keys.length / 2); canvas.height = whiteKeyHeight;,
Updated src/drawkeyboard.ts added const margin = 0.1; const availableWidth = window.innerWidth * (1 - 2 * margin); const availableHeight = window.innerHeight * (1 - 2 * margin);,
Updated src/drawkeyboard.ts added canvas.width = availableWidth; canvas.height = availableHeight;,
Updated src/drawkeyboard.ts added const numberOfWhiteKeys = keys.filter(key => !key.includes('#')).length;,
Updated src/drawkeyboard.ts updated const whiteKeyWidth = availableWidth / numberOfWhiteKeys;,
Updated src/drawkeyboard.ts updated const whiteKeyHeight = availableHeight;,

-08_06_24
Updated src/drawkeyboard.ts updated highlightKey() { highlight.style.zIndex = '1'; if (height === b;ackKeyHeight) {highlight.style.zIndex = '2';}},
Updated src/drawkeyboard.ts updated highlightKey(..., keyType: string) { highlight.style.zIndex = keyType === 'black' ? '2' : '1';},
Updated src/drawkeyboard.ts updated marjorScalePattern.forEach() {..., highlightKey(x, 0, width, height, canvas.getBoundingClientRect().left, canvas.getBoundingClientRect().top, false, key.type);},

-08_15_24
Updated src/drawkeyboard.ts updated function highlightKey() {};,
Ran npm run build,
Updated src/drawkeyboard.ts with if (noteSelect && chordTypeSelect) {},

-08_20_24
Updated src/drawkeyboard.ts highlightKey() { const startColor = 'rgba(255, 0, 0, 1); ...},
Commented src/drawkeyboard.ts commented out // Add highlight to the key ctx.fillStyle = 'red'; ...,
Updated src/drawkeyboard.ts highlightKey() {// Redraw black keys on top if necessary  const blackKeyX = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;},
Updated src/drawkeyboard.ts hightlightKey() { currentStep++ if (currentStep <= steps { requestAnimationFrame(animate);})},

-08_21_24
Updated src/drawkeyboard.ts highlightKey() { const startColor = { r: 255, b: 0, g: 0, a: 1 };, const endColor = { r: 255, b: 0, g: 0, a: 0 };},
Updated src/drawkeyboard.ts highlightKey() { const currentColor = r: Math.round(startColor.r + progress * (endColor.r - startColor.r)), ... },

-08_23_24
Commented src/drawkeyboard.ts commented out highlightKey(x, 0,width, height, canvas.getBoundingClientRect().left, canvas.getBoundingClientRect().top, false, key.type) },
Updated src/drawkeyboard.ts // Highlight key without redrawing the keyboard ctx.fillStyle = 'red'; ...,

-08_27_24
Commented src/drawkeyboard.ts commented out  // ctx.fillRect(x , y, width, height);,
Updated src/drawkeyboard.ts highlightKey() {... ctx.lineWidth = 3;},
Commented src/drawkeyboard.ts commented out // ctx.fillRect(x, 0, width, height);,
Updated src/drawkeyboard.ts majorScalePattern.forEach(... ctx.lineWidth = 3;)

-08_28_24

-08_29_24
Commented src/drawkeyboard.ts commented out //currentStep++ if (currentStep <= steps) {...};,
Updated src/drawkeyboard.ts highlightKey() { currentStep++ if (if (currentStep <= steps) {requestAnimationFrame(animate)})},
Commented src/drawkeyboard.ts commented out // Redraw black keys on top if necessary,
Updated src/drawkeyboard.ts highlightKey() // Draw black keys on top after highlighting if (keyType === 'white') {...},
Commented src/drawkeyboard.ts // animate();
Updated src/drawkeyboard.ts highlightKey() { animate() },
Updated src/drawkeyboard.ts majorScalePattern.forEach() { if (key.type === 'white') {...},
Commented src/drawkeyboard.ts // Add event listeners to dropdowns if (noteSelect && chordTypeSelect) {},
Updated src/drawkeyboard.ts  // Add event listeners noteSelect.addEventListener('change', () => {}),
Commented src/drawkeyboard.ts // ctx.lineWidth = 3; // ctx.strokeRect(x, y, width, height);,
Commented src/drawkeyboard.ts // hightlightKey () {if(keyType === 'white') {},

-08_30_24
Updated src/drawkeyboard.ts function drawBlackKeysOnTop() {...},
Commented src/drawkeyboard.ts majorScalePattern.forEach(interval) { // ctx.strokeRect(x, 0, width, height); },
Commented src/drawkeyboard.ts majorScalePattern.forEach(interval) {// if (key.type === 'white') {...},
Updated src/drawkeyboard.ts majorScalePattern.forEach(interval) { drawBlackKeysOnTop();},

-09_04_24
Updated src/drawkeyboard.ts const steps = 40;,

-09_05_24
Updated src/drawkeyboard.ts highlightScale() { if (key.type === 'black') { ctx.fillStyle = 'black'; ctx.fillRect(x, 0, blackKeyWidth, blackKeyHeight)}};,
Updated src/drawkeyboard.ts highlightScale() { const highlightedKeys = [];},
Commented src/drawkeyboard.ts highlightScale() {   // // If it's a black key, draw it again on top to maintain correct layering},
Updated src/drawkeyboard.ts hightlightScale() {..., const highlightedKeys: {x: number; width: number; height: number; type: string;}[] = [];},
Updated src/drawkeyboard.ts if () { highlightedKeys.push({ x, width, height, type: key.type  });},
Updated src/drawkeyboard.ts if (key.type === 'white') {...},
Updated src/drawkeyboard.ts  // Now highlight black keys after they've been drawn,

-09_06_24
Removed src/drawkeyboard.ts const interval = duration / steps; const duration = 200; // Duration of the effect in milliseconds,
Updated src/drawkeyboard.ts function highlightChord() {},
Updated src/drawkeyboard.ts function highlightChord() {if (!ctx) {}; ...},
Updated src/drawkeyboard.ts function highlightChord() { const noteIndices = { 'C': 0, ... },
Updated src/drawkeyboard.ts function highlightChord() { const majorChordPattern = [0, 4, 7]; },
Updated src/drawkeyboard.ts function highlightChord() { const startIndex = noteIndices[note as keyof typeof noteIndices]; },
Updated src/drawkeyboard.ts function highlightChord() { const highlightedKeys: {x: number; width: number; height: number; type: string;}[] = []; },
Updated src/drawkeyboard.ts function highlightChord() { majorChordPattern.forEach(interval => {}) },
Updated src/drawkeyboard.ts function highlightChord() { // Calculate the key index for the note within the chromatic scale const keyIndex = (startIndex + interval) % 12; },
Updated src/drawkeyboard.ts function highlightChord() { const octave = Math.floor((startIndex + interval) / 12); },
Updated src/drawkeyboard.ts function highlightChord() { const actualKeyIndex = keyIndex + octave * 12;},
Updated src/drawkeyboard.ts function highlightChord() { if (key) { const x = key.x; ... },
Updated src/drawkeyboard.ts function highlightChord() { if (key.type === 'white') {} },
Updated src/drawkeyboard.ts function highlightChord() { drawBlackKeysOnTop(); },
Updated src/drawkeyboard.ts function highlightChord() { highlightedKeys.forEach(({ x, width, height, type }) => { } },
Commented src/drawkeyboard.ts // chordTypeSelect.addEventListener('change') {},
Updated src/drawkeyboard.ts function highlightChord() { console.log(`Highlighting chord for note: ${note}, chord type: ${chordType}`); },
Updated src/drawkeyboard.ts chordTypeSelect.addEventListener('change', () => { highlightChord(note, chordType); },

-09_09_24

-09_12_24
Commented src/drawkeyboard.ts chordTypeSelect.addEventListener('change', () => { highlightChord(note, chordType); },
Updated src/drawkeyboard.ts highlightChord() { let chordPattern: number[] = []},
Updated src/drawkeyboard.ts highlightChord() { switch(chordType) case 'major' : chordPattern = [0, 4, 7], ..., default: chordPattern = [0, 4, 7];},
Commented src/drawkeyboard.ts highlighChord() {majorChordPattern = []},
Commented src/drawkeyboard.ts highlightChord() { majorChordPattern.forEach() {...}},
Updated src/drawkeyboard.ts highlightChord() {chordPattern.forEach( {...})},

---------------------------------------------------------------------

Backup of code

-07_19_24

<script>
        document.addEventListener('DOMContentLoaded', () => {
            const canvas = document.getElementById('piano');
            const ctx = canvas.getContext('2d');

            const whiteKeyWidth = 50;
            const whiteKeyHeight = 200;
            const blackKeyWidth = 30;
            const blackKeyHeight = 120;

            const keys = [
                'C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B',
                'C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'
            ];

            // Draw white keys
            for (let i = 0; i < keys.length; i++) {
                if (!keys[i].includes('#')) {
                    ctx.fillStyle = 'white';
                    ctx.fillRect(i * whiteKeyWidth / 2, 0, whiteKeyWidth, whiteKeyHeight);
                    ctx.strokeRect(i * whiteKeyWidth / 2, 0, whiteKeyWidth, whiteKeyHeight);
                }
            }

            // Draw black keys
            for (let i = 0; i < keys.length; i++) {
                if (keys[i].includes('#')) {
                    ctx.fillStyle = 'black';
                    ctx.fillRect((i - 1) * whiteKeyWidth / 2 + whiteKeyWidth - blackKeyWidth / 2, 0, blackKeyWidth, blackKeyHeight);
                }
            }
        });
    </script>

-07_22_24
<script>
        document.addEventListener('DOMContentLoaded', () => {
            const canvas = document.getElementById('piano');
            const ctx = canvas.getContext('2d');

            const whiteKeyWidth = 50;
            const whiteKeyHeight = 200;
            const blackKeyWidth = 30;
            const blackKeyHeight = 120;

            const keys = [
                'C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B',
                'C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'
            ];

            let whiteKeyIndex = 0;

            // Draw white keys
            for (let i = 0; i < keys.length; i++) {
                if (!keys[i].includes('#')) {
                    ctx.fillStyle = 'white';
                    ctx.fillRect(whiteKeyIndex * whiteKeyWidth, 0, whiteKeyWidth, whiteKeyHeight);
                    ctx.strokeRect(whiteKeyIndex * whiteKeyWidth, 0, whiteKeyWidth, whiteKeyHeight);
                    whiteKeyIndex++;
                }
            }

            whiteKeyIndex = 0;

            // Draw black keys
            for (let i = 0; i < keys.length; i++) {
                if (!keys[i].includes('#')) {
                    whiteKeyIndex++;
                } else {
                    ctx.fillStyle = 'black';
                    ctx.fillRect(whiteKeyIndex * whiteKeyWidth - blackKeyHeight / 2, 0, blackKeyWidth, blackKeyHeight);
                }
            }
        });
    </script>

-08_01_24
let whiteKeyIndex = 0;
    const keyElements: {[key: number]: { type: string, x: number, width: number } } = {};

    // Draw white keys
    for (let i = 0; i < keys.length; i++) {
        const x = whiteKeyIndex * whiteKeyWidth;
        if (!keys[i].includes('#')) {
            ctx.fillStyle = 'white';
            ctx.fillRect(x, 0, whiteKeyWidth, whiteKeyHeight);
            ctx.strokeRect(x, 0, whiteKeyWidth, whiteKeyHeight);
            keyElements[i] = { type: 'white', x, width: whiteKeyWidth };
            whiteKeyIndex++;
        }
    }

    // Reset value to 0
    whiteKeyIndex = 0;

    // Draw black keys
    for (let i = 0; i < keys.length; i++) {
        if (!keys[i].includes('#')) {
            whiteKeyIndex++;
        } else {
            const x = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;
            ctx.fillStyle = 'black';
            ctx.fillRect(x, 0, blackKeyWidth, blackKeyHeight);
            keyElements[i] = { type: 'black', x, width: blackKeyWidth };
        }
    }


    -08_05_24
        // Set canvas dimensions
    canvas.width = whiteKeyHeight * (keys.length / 2);
    canvas.height = whiteKeyHeight;


    // let whiteKeyIndex = 0;
    // const keyElements: {[key: number]: { type: string, x: number, width: number } } = {};

    // // Draw white keys
    // for (let i = 0; i < keys.length; i++) {
    //     const x = whiteKeyIndex * whiteKeyWidth;
    //     if (!keys[i].includes('#')) {
    //         ctx.fillStyle = 'white';
    //         ctx.fillRect(x, 0, whiteKeyWidth, whiteKeyHeight);
    //         ctx.strokeRect(x, 0, whiteKeyWidth, whiteKeyHeight);
    //         keyElements[i] = { type: 'white', x, width: whiteKeyWidth };
    //         whiteKeyIndex++;
    //     }
    // }

    // // Reset value to 0
    // whiteKeyIndex = 0;

    // // Draw black keys
    // for (let i = 0; i < keys.length; i++) {
    //     if (!keys[i].includes('#')) {
    //         whiteKeyIndex++;
    //     } else {
    //         const x = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;
    //         ctx.fillStyle = 'black';
    //         ctx.fillRect(x, 0, blackKeyWidth, blackKeyHeight);
    //         keyElements[i] = { type: 'black', x, width: blackKeyWidth };
    //     }
    // }

            // // Redraw the keyboard
        // for (let i = 0; i < keys.length; i++) {
        //     const x = keyElements[i].x;
        //     if (keyElements[i].type === 'white') {
        //         ctx.fillStyle = 'white';
        //         ctx.fillRect(x, 0, keyElements[i].width, whiteKeyHeight);
        //         ctx.strokeRect(x, 0, keyElements[i].width, whiteKeyHeight);
        //     } else {
        //         ctx.fillStyle = 'black';
        //         ctx.fillRect(x, 0, keyElements[i].width, blackKeyHeight);
        //     }
        // }

            //     // Highlight the scale
    //     majorScalePattern.forEach(interval => {
    //         const keyIndex = (startIndex + interval) % 12;
    //         const x = keyElements[keyIndex].x;
    //         const width = keyElements[keyIndex].width;
    //         const height = keyElements[keyIndex].type === 'white' ? whiteKeyHeight : blackKeyHeight;

    //         console.log(`Highlighting key index: ${keyIndex}, x: ${x}, width: ${width}, height: ${height}`);

    //     // Highlight using the highlightKey function without the temporary effect
    //     highlightKey(x, 0,width, height, canvas.getBoundingClientRect().left, canvas.getBoundingClientRect().top, false);

    //     });
    // }

    -08_15_24
    // Create a temporary element to add the animation effect
        const highlight = document.createElement('div');
        highlight.style.position = 'absolute';
        highlight.style.left = `${canvasLeft + x}px`;
        highlight.style.top = `${canvasTop + y}px`;
        highlight.style.width = `${width}px`;
        highlight.style.height = `${height}px`;
        highlight.style.border = '2px solid red';
        highlight.className = 'animate-highlight';

        // // Adjust z-index based on the key type
        // highlight.style.zIndex = '1';
        // if (height === blackKeyHeight) {
        //     highlight.style.zIndex = '2';
        // }

        // Adjust z-index based on the key type
        highlight.style.zIndex = keyType === 'black' ? '2' : '1';



        document.body.appendChild(highlight);

        // Remove the element after animation if it's temporary
        if (isTemporary) {
            setTimeout(() => {
                highlight.remove();
            }, 200);
        } else {
            // Ensure ctx is not null
            if (ctx) {
                ctx.fillStyle = 'red';
                ctx.fillRect(x, 0, width, height);
                ctx.strokeRect(x, 0, width, height);
            }
        }
    }

09_05_24
// // Draw black keys on top after highlighting
            // if (keyType === 'white') {
            //     let whiteKeyIndex = 0;
            //     for (let i = 0; i< keys.length; i++) {
            //         if (keys[i].includes('#')) {
            //             const blackKeyX = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;
            //             ctx.fillStyle = 'black';
            //             ctx.fillRect(blackKeyX, 0, blackKeyWidth, blackKeyHeight);
            //         } else {
            //             whiteKeyIndex++;
            //         }
            //     }
            // }

            // // Redraw black keys on top if necessary
            // let whiteKeyIndex = 0;
            // for (let i = 0; i < keys.length; i++) {
            //     if (!keys[i].includes('#')) {
            //         whiteKeyIndex++;
            //     } else {
            //         const blackKeyX = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;
            //         ctx.fillStyle = 'black';
            //         ctx.fillRect(blackKeyX, 0, blackKeyWidth, blackKeyHeight);
            //     }
            // }

            // currentStep++;
            // if (currentStep <= steps) {
            //     requestAnimationFrame(animate);
            // }

           
            // animate();

                        // ctx.lineWidth = 3;
            // ctx.strokeRect(x, y, width, height);


                    // // Add highlight to the key
        // ctx.fillStyle = 'red';
        // ctx.fillRect(x, y, width, height);
        // ctx.strokeRect(x, y, width, height);


        

        // // Remove temporary hightlight
        // if (isTemporary) {
        //     setTimeout(() => {
        //         drawKeyboard();
        //     }, 200);
        // }


            //     // Create a temporary element to add the animation effect
    //     const highlight = document.createElement('div');
    //     highlight.style.position = 'absolute';
    //     highlight.style.left = `${canvasLeft + x}px`;
    //     highlight.style.top = `${canvasTop + y}px`;
    //     highlight.style.width = `${width}px`;
    //     highlight.style.height = `${height}px`;
    //     highlight.style.border = '2px solid red';
    //     highlight.className = 'animate-highlight';

    //     // // Adjust z-index based on the key type
    //     // highlight.style.zIndex = '1';
    //     // if (height === blackKeyHeight) {
    //     //     highlight.style.zIndex = '2';
    //     // }

    //     // Adjust z-index based on the key type
    //     highlight.style.zIndex = keyType === 'black' ? '2' : '1';



    //     document.body.appendChild(highlight);

    //     // Remove the element after animation if it's temporary
    //     if (isTemporary) {
    //         setTimeout(() => {
    //             highlight.remove();
    //         }, 200);
    //     } else {
    //         // Ensure ctx is not null
    //         if (ctx) {
    //             ctx.fillStyle = 'red';
    //             ctx.fillRect(x, 0, width, height);
    //             ctx.strokeRect(x, 0, width, height);
    //         }
    //     }
    // }

            // ctx.clearRect(0, 0, canvas.width, canvas.height);


                // highlightKey(x, 0,width, height, canvas.getBoundingClientRect().left, canvas.getBoundingClientRect().top, false, key.type)

               // ctx.lineWidth = 3;
                // ctx.strokeRect(x, 0, width, height);

                // if (key.type === 'white') {
                //     let whiteKeyIndex = 0;
                //     for (let i = 0; i < keys.length; i++) {
                //         if (keys[i].includes('#')) {
                //             const blackKeyX = whiteKeyIndex * whiteKeyWidth - blackKeyWidth / 2;
                //             ctx.fillStyle = 'black';
                //             ctx.fillRect(blackKeyX, 0, blackKeyWidth, blackKeyHeight);
                //         } else {
                //             whiteKeyIndex++;
                //         }
                //     }
                // }

    // // Add event listeners to dropdowns
    // if (noteSelect && chordTypeSelect) {
    //     noteSelect.addEventListener('change', () => {
    //         const note = noteSelect.value;
    //         const chordType = chordTypeSelect.value;
    //         highlightScale(note, chordType);
    //     });

    //     chordTypeSelect.addEventListener('change', () => {
    //         const note = noteSelect.value;
    //         const chordType = chordTypeSelect.value;
    //         highlightScale(note, chordType);
    //     });
    // }

                    // // If it's a black key, draw it again on top to maintain correct layering
                // if (key.type === 'black') {
                //     ctx.fillStyle = 'black';
                //     ctx.fillRect(x, 0, blackKeyWidth, blackKeyHeight);
                // }